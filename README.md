# gateway-customer-crud-kafka-cqrs
Java 21 + Spring Boot 3.5, Kafka (KRaft), PostgreSQL, Flyway. Command (OLTP + Outbox) - Kafka events - Projection (read model). Manual offsets, retries, DLT, idempotent versioning.
